---
import { lang } from 'astro:i18n';
import {template} from "../../settings";

const currentLang = lang || "fr";
---
<div>{currentLang}</div>
<label class="grid cursor-pointer place-items-center swap swap-rotate" transition:persist>
	<input
			type="checkbox"
			id="lang-toggle"
			value={currentLang}
			data-choose-theme
	/>

	<div class="swap-on">🇫🇷</div>
	<div class="swap-off">🇺🇸</div>
</label>

